# Build output
/build/
compile_commands.json

# OMF binaries (generated by merlin32, or copied-in test corpus from other,
# possibly private, projects) - never track these.
*.s16
*.sys16
*.o
_FileInformation.txt

# Test corpus copied from other projects - kept local only, never pushed.
/tests/fixtures/
/tests/FIXTURES.md

# Editor / OS
.DS_Store
